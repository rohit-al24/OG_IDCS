# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database Settings (for PostgreSQL in production)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_HOST=localhost
DB_PORT=5432

# Email Settings
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Static and Media Files (for production)
STATIC_ROOT=/path/to/static
MEDIA_ROOT=/path/to/media

# Optional: Supabase/Postgres connection example (do NOT commit real credentials)
# Example full DATABASE_URL format (preferred):
# DATABASE_URL='postgresql://postgres:password@db.pahfkfelkfamzycdioeb.supabase.co:5432/postgres'
# If you prefer REST upload instead of direct DB access, set:
# SUPABASE_URL='https://pahfkfelkfamzycdioeb.supabase.co'
# SUPABASE_SERVICE_ROLE='your-service-role-key-here'
